# Required.
OPENAI_API_KEY=sk-...

# Optional. USD cap the `serve` subcommand uses if --budget isn't passed.
# The cap is shared across all requests for the lifetime of the process.
PGQA_BUDGET_USD=1.00
